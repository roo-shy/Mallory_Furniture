<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Product List</title>
  </head>
  <body>
    <nav>
    <li>
      <a href="http://localhost:3000/">
        <span class="glyphicon glyphicon-home" aria-hidden="true"></span>
      </a>
    </li>
    <li>
      <a href="http://localhost:3000/">
        <span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span>
      </a>
    </li>
  </nav>
  <div class="container">
    <div class="row">
      <% @products.each do |product| %>
      <div class="col-md-3">
        <div class="product-box">
          <%= image_tag product.img_file, class: "img-responsive"%>
          <span><%= product.item %></span>
          <br>
          <h2>Price</h2>
          <p>$<% if product.condition.to_s == "good" %>
            <%= product.discount %>
            <span class="highlight">
              <b>Clearance Price</b>
            </span>
            <% elsif product.condition.to_s == "average" %>
            <%= product.discount %>
            <span class="highlight">
              <b>Clearance Price</b>
            </span>
          <% else %>
            <%= product.price %>
            <% end %>
          </p>
          <%= link_to "More Info", "/products/#{product.pid}", class:"btn btn-default"%>
        </div>
      </div>
      <% end %>

    </div>
  </div>

</body>
</html>
